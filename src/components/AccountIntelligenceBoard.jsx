import { Activity, ChevronRight } from 'lucide-react'

export default function AccountIntelligenceBoard({ accounts = [], activeId, candidates = [], trends = [], onSelect }) {
  return <section className="account-intelligence-board"><div className="account-intelligence-board-head"><div><span className="eyebrow"><Activity size={13} /> ACCOUNT INTELLIGENCE</span><h2>账号情报看板</h2><p>选择账号，热点、候选池和复盘数据会同步切换。</p></div><span className="queue-count">{accounts.length} 个账号</span></div><div className="account-intelligence-cards">{accounts.map((account) => { const accountCandidates = candidates.filter((item) => item.accountId === account.id); const accountTrends = trends.filter((item) => item.category === account.category); return <button key={account.id} className={`account-intelligence-card ${activeId === account.id ? 'account-intelligence-active' : ''}`} onClick={() => onSelect(account.id)}><span className="managed-avatar">{account.avatar}</span><span className="account-intelligence-copy"><strong>{account.name}</strong><small>{account.categoryLabel} · 健康度 {account.health} · {account.timeZone || 'America/Los_Angeles'}</small><span><b>{accountTrends.length}</b> 个热点 <b>{accountCandidates.length}</b> 条候选 · {account.targetAudience || '待补充目标人群'}</span><em>{(account.contentPillars || [account.categoryLabel]).join(' / ')}</em></span><ChevronRight size={15} /></button> })}</div></section>
}
